#!/bin/bash

# Use this command to build the kernel for the host machine.

source /etc/portage/make.conf

/usr/cryptoo/bin/kmake_host ${MAKEOPTS} &&
/usr/cryptoo/bin/kmake_host install &&
/usr/sbin/grub2-mkconfig > /boot/grub/grub.cfg

