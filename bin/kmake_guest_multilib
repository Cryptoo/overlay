#!/bin/bash

# Use this command as an alias for "make" when configuring and building
# the kernel for the host machine.

export KTYPE="guest_multilib"
export KARCH="x86_64"

cd /usr/src/linux
exec make "$@"
